import { API_BASE_URL } from '../../../config/api.js';import { getAuthToken } from '../../../services/auth/authStorage.js';const base=`${API_BASE_URL}/v1/admin/commission-settlement`;async function call(path='',options={}){const r=await fetch(base+path,{...options,headers:{Authorization:`Bearer ${getAuthToken()}`,...(options.body?{'Content-Type':'application/json'}:{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||'Commission action failed.');return d.data;}export const settlementQueue=p=>call(`?${new URLSearchParams(Object.entries(p||{}).filter(([,v])=>v!==''&&v!=null))}`);export const settlementStats=()=>call('/stats');export const commissionAction=data=>call('/approval',{method:'POST',body:JSON.stringify(data)});export const releaseCommission=data=>call('/payment',{method:'POST',body:JSON.stringify(data)});export const commissionHistory=id=>call(`/history/${id}`);
