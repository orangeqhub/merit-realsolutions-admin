import {useEffect,useState}from'react';import{motion}from'framer-motion';import PageHeader from '../../../components/layout/PageHeader';import KPIGrid from '../../../components/dashboard/KPIGrid';import ChartCard from '../../../components/charts/ChartCard';import BarChart from '../../../components/charts/BarChart';import DonutChart from '../../../components/charts/DonutChart';import Input from '../../../components/ui/input/Input';import Select from '../../../components/ui/select/Select';import DataTable from '../../../components/table/DataTable';import Button from '../../../components/ui/button/Button';import {getSalesPerformanceDashboard}from './salesPerformanceApi.js';import '../commissions/commission.css';
const money=v=>`₹${Number(v||0).toLocaleString('en-IN')}`;export default function SalesPerformanceDashboard(){const[data,setData]=useState(null),[loading,setLoading]=useState(true),[filters,setFilters]=useState({from:'',to:''}),[error,setError]=useState('');const load=()=>{setLoading(true);getSalesPerformanceDashboard(filters).then(setData).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);const k=data?.kpis||{};return <motion.div className="erp-module-page commission-page" initial={{opacity:0}} animate={{opacity:1}}><PageHeader title="Sales Performance Dashboard" description="Executive insights across bookings, revenue, commissions, and sales-team activity." actions={<Button variant="ghost" onClick={()=>window.print()}>Print Dashboard</Button>}/><section className="commission-filters partner-crm-panel"><Input type="date" label="From" value={filters.from} onChange={e=>setFilters(f=>({...f,from:e.target.value}))}/><Input type="date" label="To" value={filters.to} onChange={e=>setFilters(f=>({...f,to:e.target.value}))}/><Button onClick={load}>Apply filters</Button></section>{error&&<p>{error}</p>}<KPIGrid items={[{label:"Today's Leads",value:k.todayLeads||0,tone:'primary'},{label:"Today's Bookings",value:k.todayBookings||0,tone:'success'},{label:'Monthly Bookings',value:k.monthlyBookings||0,tone:'accent'},{label:'Revenue This Month',value:money(k.revenueThisMonth),tone:'success'},{label:'Commission Generated',value:money(k.commissionGenerated),tone:'violet'},{label:'Commission Paid',value:money(k.commissionPaid),tone:'primary'},{label:'Outstanding Commission',value:money(k.outstandingCommission),tone:'warning'},{label:'Conversion Rate',value:`${k.conversionRate||0}%`,tone:'info'}]}/>{loading?<p>Loading performance dashboard…</p>:<><div className="sales-performance-dashboard__charts"><ChartCard title="Sales Funnel" subtitle="Lead progression"><BarChart data={data?.funnel||[]}/></ChartCard><ChartCard title="Commission Analytics" subtitle="By workflow status"><DonutChart data={data?.commission||[]}/></ChartCard><ChartCard title="Booking Analytics" subtitle="By booking status"><BarChart data={data?.bookings||[]}/></ChartCard><ChartCard title="Property Inventory" subtitle="Current availability"><DonutChart data={data?.inventory||[]}/></ChartCard></div><section className="partner-crm-panel"><h3>Sales Leaderboard</h3><DataTable paginated={false} columns={[{key:'rank',header:'Rank'},{key:'employee',header:'Employee',render:r=>r.employee?.name||'—'},{key:'role',header:'Role',render:r=>r.employee?.role?.replaceAll('_',' ')},{key:'bookings',header:'Bookings'},{key:'commission',header:'Commission',render:r=>money(r.commission)}]} data={data?.leaderboard||[]}/></section></>}</motion.div>}
